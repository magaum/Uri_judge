#include <stdio.h>

int main() {
        
        float raio;
        double area, II = 3.14159;
                scanf("%.2f", &raio);
                        area = II * (raio * raio);
                printf("A=%.4f\n", area);
        return 0;
}
