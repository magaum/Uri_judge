#include <stdio.h>

int main() {

	double c, c1, MEDIA_TOTAL;

    scanf("%lf", & c);
		scanf("%lf", & c1);
     MEDIA_TOTAL = (((c * 3.5)+(c1 * 7.5))/11);
		printf("MEDIA = %.5f\n", MEDIA_TOTAL);

	return 0;
}
//Accepted
